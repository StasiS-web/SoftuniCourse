'use strict'

function concatenateData([firstName, lastName, age, town]) {
    console.log(`You are ${firstName} ${lastName}, a ${age}-years old person from ${town}.`);
}

concatenateData(['John', 'Smith', 20, 'Sofia']);